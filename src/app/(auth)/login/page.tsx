import LoginPanel from "@/components/login/loginPage";
export default function LoginPage(){
    return (
        <main className="bg-white  w-[100vw] h-[100vh] flex items-center justify-center">
            <LoginPanel/>
        </main>
    );
}